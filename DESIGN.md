# DESIGN

## 현재 구조
- src/app/page.tsx: 한국어 시작 화면
- src/app/layout.tsx: 페이지 언어(ko), 제목, 공통 레이아웃
- src/app/globals.css: 반응형 스타일. 시스템 글꼴을 사용하여 빌드 시 외부 글꼴 다운로드가 필요 없다.
- public/: 향후 이미지·로고를 넣을 폴더
- .env: 현재 빈 비밀값 양식. Git에서 제외

## 다음 설계
앱 주제가 정해지면 화면 목록, 사용자 동작, 데이터 형태, 예외 처리를 추가한다.
API 키는 서버에서만 사용한다. DB·로그인·챗봇은 아직 연결되지 않았다.
