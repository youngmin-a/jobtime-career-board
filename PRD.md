# PRD — 취준캘린더 개인 취업 캘린더

기준일: 2026-09-09 · v2 구현 기준 · 시간대: Asia/Seoul(KST)

## 1. 제품 정의와 사용자
취업준비생이 직접 선택하거나 등록한 공고의 모집 일정, 전형 진행, 개인 일정을 관리한다. 기업 전체 공고를 자동 저장하지 않으며 백그라운드 최신 공고 갱신은 하지 않는다. 공식 조회는 공고를 추가할 때만 사용한다.

## 2. 확정된 런타임과 저장소
실제 기존 코드와 운영 D1을 확인해 Vinext 1.0.0-beta.5, React 19.2.6, Cloudflare Workers(nodejs_compat), D1 바인딩 DB를 유지한다. D1이 유일한 운영 영속 저장소다.
- v1: app_state(id, payload, updated_at). 원본을 수정·삭제하지 않는다.
- v2 레거시: job_state_v2(id=1, payload, revision). 기존 운영 데이터의 읽기 원본으로 보존한다.
- 현재: workspace_state(workspace_id, payload, revision, updated_at). 이름으로 연 개인 작업공간의 공고·전형·개인 일정 유일한 운영 상태다.
- 보관용: workspace_backups(id, workspace_id, payload, reason, created_at, revision). 작업공간별 이전 원본과 변경 전 스냅샷.
- 이름 공간: named_spaces(workspace_id, name_key, display_name), name_sessions(token_hash, workspace_id, session_tag, expires_at), name_imports(source_workspace_id, target_workspace_id, import_id, created_at). 같은 이름은 같은 D1 작업공간으로 연결하고, 요청은 만료 가능한 HttpOnly 세션 토큰으로 검증한다.
- 로컬 개발은 Wrangler의 로컬 D1 SQLite(.wrangler/state/v3/d1)를 사용한다. 운영 DB와 자동 동기화하지 않는다.
- .local-data/jobs.json은 사용하지 않는다. JSON 내보내기는 사용자가 다운로드하는 백업이며 운영 이중 저장이 아니다.
- 로그인 없이 링크를 가진 누구나 앱을 열 수 있다. 첫 화면에서 이름 또는 별명만 입력하면 같은 이름의 D1 작업공간을 열고, 무작위 HttpOnly·Secure·SameSite 세션 쿠키와 서버 세션 태그로 CRUD 범위를 검증한다. 비밀번호·이메일·전화번호는 받지 않는다. 기존 브라우저 쿠키 작업공간이 있으면 사용자가 버튼을 눌러서만 현재 이름 공간으로 가져오며 원본과 백업을 보존한다. 요청 본문의 ownerId나 관리자 여부를 신뢰하지 않으며 다른 이름 공간의 공고·전형·일정·메모를 읽거나 수정할 수 없다.

## 3. 공고 등록·수정·삭제
기업명과 공고명만 필수다. 직무·채용 구분·모집 시작일과 마감일·각 시각·HTTPS URL·메모는 선택한다. 미지원 기업도 직접 등록한다.
임의 기업명·채용 키워드는 2026년 하반기(7월 1일~12월 31일) 후보만 조회한다. 제목의 하반기 회차, 공식 원문의 회차, 또는 접수 시작일로 범위를 판정하며 마감된 공고도 포함한다. 공개 검색은 페이지당 50개, 최대 10페이지(500개)까지 추가 조회할 수 있다. KB국민은행·IBK기업은행·NH농협은행 공식 조회와 우리은행 공식 마감 원문을 함께 통합한다. 검색 결과를 선택한 뒤 편집 화면에서 확인하고 저장한다. 저장 시 서버가 공식 출처를 재확인한다. 자동으로 다른 후보를 저장하지 않는다.
동일 URL 또는 동일 기업명·공고명은 중복 경고를 표시하고 별도 차수·직무로 명시적 등록을 허용한다. 공식 일정을 사용자가 고쳐도 최초 공식 일정과 근거를 보존하고 되돌리기를 제공한다.
삭제는 연결 전형·일정의 영향 확인 후 원자적으로 수행하며 변경 전 스냅샷을 보관한다.

## 4. 전체 관리 상태와 전형
전체 상태: 관심, 지원 준비, 지원 중, 최종합격, 불합격, 지원 철회.
전형 상태: 미진행, 예정, 진행 중, 완료, 합격, 불합격, 해당 없음.
기본 전형: 서류 제출, AI 역량검사, 필기 전형, 1차 면접, 2차 면접, 최종면접. 사용자가 이름 수정·추가·삭제·위아래 이동을 할 수 있다.
현재 전형은 적용 전형 중 하나 또는 미선택이다. 해당 없음으로 바꾸려면 현재 전형을 먼저 해제한다. 해당 없음 전형의 저장 정보는 유지하되 캘린더와 진행 분모에서 제외한다.
제출·응시 완료를 합격으로 판단하지 않는다. 현재 전형 변경, 합격·불합격 결과, 모집 마감은 다른 전형이나 전체 상태를 자동 변경하지 않는다.

## 5. 일정과 날짜 규칙
전형별 시작·종료 일정, 미확정·예정 여부, 결과 발표 예정일, 실제 결과 확인일, 메모를 관리한다. 단일 예정일은 시작일만 입력한다. 공고에 연결된 개인 일정은 제목과 최소 한 날짜를 입력하고 추가·수정·삭제한다.
날짜는 YYYY-MM-DD, 시각은 HH:mm 또는 null, 시간대는 Asia/Seoul이다. 공식 원본의 초 값은 보존한다. 날짜가 없으면 시각도 없다. 임의의 00:00·23:59를 저장하지 않는다.
시각이 없는 마감은 당일 D-day/시간 미공개이며 다음 KST 날짜부터 모집 마감으로 분류한다. 정확한 마감 시각이 있으면 그 시각 이후 마감으로 분류한다. 카드에는 D-day와 정확한 모집 마감 시각을 함께 표시한다.
시작이 종료보다 늦으면 저장을 거부한다. 모집 기간과 전형 기간·개인 일정을 분리한다.

## 6. 대시보드
전체·지원 중·3일 이내 마감·최종합격 수와 다음 일정 4개를 간결하게 제공한다. 공고 카드의 빠른 현재 전형·진행 상태 선택과 서류 제출 완료 버튼은 상세 입력을 열지 않고 즉시 저장한다. 최종합격은 확인하고 종료/보관은 상세에서 불합격·지원 철회를 명시적으로 선택한다. 빠른 저장 실패 시 해당 변경만 원복하고 재시도한다. 기업·공고명 검색과 마감/최근 수정 정렬을 제공한다. 전형별 대형 가로 컬럼은 기본 화면에 두지 않는다.
카드에는 기업명·공고명·전체 상태·D-day·모집 시작/마감 시각·현재 전형·다음 일정·완료 및 결과 확인 전형 수를 표시한다. 기업과 전체 상태 필터를 제공한다. 모집 마감 뒤에도 지원 중인 공고는 유지된다.

## 7. 취업 캘린더
월간·주간·일정 목록, 오늘/이전/다음/기준 날짜 이동, 기업 단일 선택 및 일정 유형 복수 필터를 제공한다.
모집 시작·모집 마감·시험·면접·기타 전형·결과 예정·결과 확인·개인 일정을 구분한다. 시험과 면접 분류는 사용자가 설정한 전형명에 따른다.
월간은 7열 6주다. 모집 기간 중간 날짜에는 이벤트를 생성하지 않는다. 실제 다일 전형·개인 일정만 포함되는 날짜에 표시한다. PC 셀은 3개와 더보기, 모바일 셀은 날짜와 개수를 보여주며 날짜 선택 시 아래 목록에서 모든 일정을 연다.
주간은 PC 7열 일별 목록, 모바일 날짜별 세로 목록이다. 시간 축 위에 겹쳐 놓는 방식 대신 각 일정의 시각을 표시해 모두 읽을 수 있도록 한다. 목록은 선택한 월과 겹치는 일정을 날짜 순서로 표시한다.
일정 클릭은 해당 공고의 동일 상세 편집기를 열고 관련 전형·개인 일정으로 이동한다. 저장 후 대시보드와 캘린더가 같은 서버 응답에서 갱신된다. 캘린더 파생 이벤트를 따로 저장하지 않는다.
현재 탭·필터는 화면 상태이며 새로고침 시 초기화된다. 제품 데이터는 D1에서 복원된다. `/api/session`은 이름 진입·전환·선택적 기존 기록 가져오기를 담당한다.

## 8. 저장 신뢰성과 이전
최초 계정 작업공간 읽기 시 기존 v2 또는 v1을 검증 → workspace_backups에 원본 백업 → workspace_state 삽입을 D1 batch로 실행 → 재조회 및 검증한다. app_state와 job_state_v2는 동결 원본으로 남긴다. 이름 공간은 빈 상태로 시작하며 기존 브라우저 작업공간은 사용자의 명시적 가져오기 요청과 name_imports 중복 방지 조건이 있을 때만 병합한다. 이미 workspace_state가 있으면 재이전하지 않는다.
기존 공고 ID·기업·제목·URL·모집 날짜/시각/초·메모·공식 근거를 보존한다. 기존 지원 완료는 전체 지원 중과 서류 제출 완료로 변환한다. 다른 전형 날짜·결과는 생성하지 않는다.
매 저장은 workspace_id와 revision 조건부 갱신, 변경 전 백업을 한 트랜잭션으로 처리한다. 충돌은 409로 거부하며 입력을 유지하고 최신 상태를 불러온다. 사용자가 확인 후 다시 저장한다. 실패·손상 데이터를 빈 데이터로 덮어쓰지 않는다.
데이터 내보내기와 운영 백업 복구 절차는 MIGRATION.md를 따른다.

## 9. 품질과 인수 기준
- 필수 두 필드만 저장, 시작일만/마감일만/시각 미공개 보존
- 이름만으로 동일 작업공간 재접속, 작업공간 간 조회·수정 격리, 기존 브라우저 기록 선택적 가져오기
- 2026년 하반기·마감 포함 검색, 추가 페이지와 공식 원문 재검증
- 수동 CRUD와 공식 후보 검색·단일 선택 유지
- 서류 완료와 합격 분리, 현재 전형·해당 없음 불변식
- 전형 순서 변경 시 ID·일정·메모 보존
- 월말·연말·KST 날짜 경계, 공통 캘린더 파생 검증
- 새로고침·프로세스 재실행 후 데이터 유지
- v1 백업·이전·손상 데이터 거부·쓰기 실패 원복·동시 수정 거부
- PC/모바일 화면과 상세 저장
- test, typecheck, lint, build
실행 결과와 미검증 항목은 CHECK.md에 사실대로 기록한다.

## 10. 제외 범위
알림 발송, 외부 캘린더 동기화, 자동 지원, AI 분석, 범용 크롤러, 팀 공유, 시간표 드래그, URL 기반 화면 복원은 이번 구현에 포함하지 않는다.

