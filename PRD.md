# PRD — JOBTIME 개인 취업 캘린더

상태: 기능 확장 설계 완료, 단계적 구현 대기
문서 기준일: 2026-09-07
기준 시간대: KST(Asia/Seoul, UTC+9)

## 1. 제품 정의

JOBTIME은 취업준비생이 자신이 지원하거나 지원할 채용공고, 공고별 전형 진행 상태, 모집·시험·면접·결과 일정을 한곳에서 관리하는 개인 취업 캘린더다. 사용자가 선택하거나 직접 등록한 공고만 개인 보드에 들어가며, 대시보드와 캘린더는 같은 원본 데이터를 서로 다른 관점으로 보여준다.

제품의 중심 개념은 다음 세 가지다.

1. 내가 관리하는 채용공고
2. 공고별 전체 관리 상태와 개별 전형 진행 상태
3. 모집 일정과 전형 일정이 합쳐진 캘린더

## 2. 주요 사용자와 목표

- 여러 기업과 여러 전형을 동시에 준비하는 개인 취업준비생
- 정확한 모집 마감 시각과 다음 시험·면접 일정을 놓치고 싶지 않은 사용자
- 공식 조회가 지원되지 않는 지역수협, 지방은행, 금융기관, 일반 기업도 직접 관리하려는 사용자
- 로그인과 협업 없이 자신의 PC에서 먼저 사용하는 사용자

사용자의 핵심 목표는 “다음에 무엇을 언제 준비해야 하는지 10초 안에 확인하는 것”이다.

## 3. 해결할 문제

- 모집 마감, AI 역량검사, 필기, 면접, 결과 발표가 서로 다른 사이트와 메모에 흩어진다.
- 모집이 끝난 뒤에도 전형은 계속되므로 모집 상태와 지원 진행 상태를 하나의 값으로 표현할 수 없다.
- ‘서류 제출 완료’와 ‘서류 합격’처럼 행동 완료와 결과 확정을 혼동하기 쉽다.
- 공식 조회 지원 기업만 관리하면 실제 지원 포트폴리오를 완성할 수 없다.
- 같은 기업의 관련 없는 모든 공고가 자동 노출되면 개인 일정의 우선순위가 흐려진다.
- 날짜만 공개된 일정에 임의의 시각을 넣으면 D-day와 마감 판단이 부정확해진다.

## 4. 제품 원칙

- 사용자가 선택하거나 직접 등록한 공고만 개인 보드에 저장한다.
- 공식 조회와 수동 등록은 서로 보완하며 어느 한쪽이 다른 쪽을 대체하지 않는다.
- 모집 일정, 전형 일정, 개인 일정의 의미와 출처를 구분한다.
- 앱은 합격, 불합격, 다음 전형 진출을 추론하지 않는다.
- 공개되지 않은 날짜와 시각을 추정하지 않는다.
- 모든 날짜 계산과 표시는 KST를 기준으로 한다.
- 색상만으로 상태를 표현하지 않고 항상 텍스트 라벨을 함께 쓴다.
- 데이터 구조와 마이그레이션을 먼저 안정화한 뒤 캘린더 UI를 구현한다.

## 5. 실제 구현 상태

### 5.1 현재 구현됨

- KB국민은행, IBK기업은행, NH농협은행의 공식 채용 사이트 검색
- 기업명 또는 지원되는 공식 공고 URL 입력
- 검색 결과 중 단일 공고 선택 후 개인 보드 등록
- 선택하지 않은 공고의 자동 등록 방지
- 모집 시작·마감 일시, D-day, 공식 출처, 확인 시각 표시
- 공고별 상태 `관심 / 지원 준비 / 지원 완료`, 메모, 삭제
- 반응형 개인 공고 보드
- 공식 도메인 허용 목록과 요청 출처 검증

### 5.2 아직 구현되지 않음

- 지원되지 않는 기업의 수동 공고 등록 UI와 전체 필드 수정
- 새 전체 관리 상태와 기존 상태 마이그레이션
- 기본 전형 템플릿, 사용자 지정 전형, 현재 단계
- 전형 일정, 결과 일정, 개인 일정
- 월간·주간·목록형 캘린더
- 대시보드 통계, 필터, 공고 상세 화면
- 로컬 JSON v1에서 확장 구조로의 백업·마이그레이션
- 내보내기

### 5.3 확인된 기술 충돌

기존 문서는 Next.js App Router와 `.local-data/jobs.json` 기반 로컬 MVP를 정의했지만, 현재 코드와 배포본은 Vinext 기반 App Router 호환 구조와 D1 단일 상태 레코드를 사용한다. 이번 요청에서는 코드를 수정하지 않는다. 다음 구현 착수 전에 아래 중 하나를 명시적으로 결정해야 한다.

- **로컬 MVP 우선:** Next.js와 `.local-data/jobs.json` 저장소로 복귀하고 이 문서의 백업·원자적 교체 규칙을 구현한다.
- **현재 배포본 유지:** Vinext와 D1을 유지하고 로컬 JSON 마이그레이션을 가져오기 기능으로 바꾼다.

본 문서의 기능 요구는 두 저장 방식에 공통이다. 저장·마이그레이션 절은 사용자가 요청한 로컬 JSON MVP를 기준 설계로 둔다.

## 6. 핵심 기능

### 6.1 공식 공고 검색과 선택

- `내 공고 추가`에서 `공식 공고 검색`을 선택한다.
- 기업명 또는 지원되는 공식 공고 URL을 입력한다.
- 기업명 검색은 해당 기업의 공식 공고 후보를 보여준다.
- 공식 URL 검색은 URL의 공식 도메인과 공고 식별값을 검사해 일치 후보를 우선한다.
- 사용자가 후보 하나를 선택한 때에만 개인 보드에 저장한다.
- 지원 범위:

| 기업 | 조회 범위 | 일정 추출 원칙 |
|---|---|---|
| KB국민은행 | 공식 신입 채용 사이트 | 공식 데이터의 접수 시작·종료 필드 사용 |
| IBK기업은행 | 공식 신입·수시 채용 사이트 | 상세 본문의 서류접수·입행지원서 접수 구간 사용 |
| NH농협은행 | 공식 신규직원 채용 메인 | 메인 공고의 모집기간 사용 |

- 게시일, 필기일, 인적성검사 기간을 모집 기간으로 재사용하지 않는다.
- 서버는 허용한 공식 도메인만 조회한다. 수동 URL은 서버 조회 대상으로 사용하지 않는다.

### 6.2 수동 공고 등록

`내 공고 추가`에서 `직접 등록`을 선택한다. 등록 대상 기업을 세 은행으로 제한하지 않는다.

필수 입력:

- 기업명
- 공고명

선택 입력:

- 모집 시작일과 시작 시각
- 모집 마감일과 마감 시각
- 공식 공고 URL
- 채용 분야 또는 직무
- 채용 구분: 신입, 경력, 인턴, 계약직, 기타
- 공고 메모
- 최초 전체 관리 상태

동작 규칙:

- 시작일과 마감일은 독립적으로 저장할 수 있다.
- 날짜가 있어도 시각을 모르면 `time: null`로 저장한다.
- URL은 비어 있을 수 있다. 값이 있으면 HTTPS와 길이를 검증한다.
- 같은 기업명을 여러 공고에서 사용할 수 있다.
- URL 또는 공식 식별값이 같으면 중복 가능성을 안내하되 사용자가 계속 등록할 수 있다.
- 등록 후 수동 입력 필드를 수정할 수 있다.
- 삭제 전 연결된 전형 수와 일정 수를 보여주고 명시적 확인을 받는다.
- 삭제는 공고, 전형, 개인 일정의 묶음을 한 번의 저장 작업으로 처리한다.

### 6.3 공고 전체 관리 상태

공고의 전체 상태는 다음 값 중 하나다.

| 상태 | 의미 |
|---|---|
| 관심 | 검토 중이며 지원 준비를 시작하지 않음 |
| 지원 준비 | 지원서·서류를 준비 중 |
| 지원 중 | 서류를 제출했고 하나 이상의 전형을 진행 중 |
| 최종합격 | 사용자가 최종 합격 결과를 확인함 |
| 불합격 | 사용자가 불합격 결과를 확인함 |
| 지원 철회 | 사용자가 지원을 중단함 |

- 모집 마감만으로 전체 상태를 바꾸지 않는다.
- 종료 상태는 사용자가 직접 선택하거나 결과 입력을 명시적으로 확인한 경우에만 설정한다.
- `지원 중`과 ‘모집 중’은 별도 개념이다.

### 6.4 개별 전형 관리

신규 공고에는 다음 기본 템플릿을 복사해 제공한다.

1. 서류 제출
2. AI 역량검사
3. 필기 전형
4. 1차 면접
5. 2차 면접
6. 최종면접

각 전형은 다음을 가진다.

- 전형명과 순서
- 적용 여부
- 진행 상태
- 현재 단계 여부
- 예정일 또는 시작·종료 일정
- 결과 발표 예정일
- 실제 결과 확인일
- 전형 메모

전형 상태:

| 상태 | 의미 |
|---|---|
| 미진행 | 아직 계획·실행되지 않음 |
| 예정 | 날짜 또는 계획이 정해짐 |
| 진행 중 | 현재 수행 중 |
| 완료 | 응시·제출을 끝냈으나 결과는 확정되지 않음 |
| 합격 | 해당 전형 합격을 확인함 |
| 불합격 | 해당 전형 불합격을 확인함 |
| 해당 없음 | 이 공고에 적용하지 않음 |

- `완료`는 `합격`이 아니다.
- 현재 단계는 적용 중인 전형 하나만 선택할 수 있다.
- 현재 단계와 전형 상태가 모순되면 저장 전에 설명과 선택지를 제공한다. 자동 수정하지 않는다.
- 전형명 수정, 추가, 삭제, 순서 변경, 해당 없음 설정을 지원한다.
- 순서 변경은 전형 ID를 유지하므로 일정·결과·메모가 사라지지 않는다.
- 전형 삭제 전 연결 일정을 안내하고 확인받는다.
- ‘다음 단계로 이동’은 제안 동작이며 사용자가 확인해야 반영된다.

### 6.5 통합 캘린더

보기:

- 월간: 월 전체 일정과 충돌 확인
- 주간: 시험·면접 시각과 준비 우선순위 확인
- 목록: 선택 기간의 일정을 날짜순으로 확인하며 모바일 기본 보기로 사용

공통 동작:

- 오늘, 이전, 다음, 날짜 선택
- 기업 필터
- 일정 유형 필터
- 일정 클릭 시 공고 상세 또는 일정 상세 패널 열기
- 같은 날 일정이 셀 한도를 넘으면 `+N개 더보기`로 목록 패널 열기

표시 일정:

- 공고의 모집 시작일
- 모집 시작일부터 마감일까지의 기간
- 모집 마감 이벤트
- 각 전형의 시작·종료 일정
- 결과 발표 예정일과 실제 결과 확인일
- 공고에 연결된 개인 일정

캘린더 이벤트는 중복 원본을 만들지 않는다. 모집 기간·마감은 공고에서, 전형 일정은 전형에서 파생한다. 사용자가 직접 추가한 개인 일정만 독립 레코드로 저장한다.

### 6.6 대시보드

대시보드는 현재 행동이 필요한 정보를 우선한다.

- 전체 관리 공고 수: 종료 상태 포함 모든 공고
- 지원 준비 수: 전체 상태가 `지원 준비`인 공고
- 지원 중 수: 전체 상태가 `지원 중`인 공고
- 3일 이내 모집 마감 수: 종료 시각이 있거나 날짜 기준으로 아직 마감되지 않은 공고
- 다음 시험·면접: 오늘 이후 가장 이른 적용 전형 일정
- 종료 현황: 최종합격, 불합격, 지원 철회 수

공고 카드는 기업명, 공고명, 모집 마감, D-day, 전체 상태, 현재 전형, 다음 일정을 우선 표시한다. 전체 전형 목록은 상세 화면에서 보여준다.

### 6.7 공고 상세

- 기업·공고·직무·채용 구분
- 모집 시작·마감 및 정보 출처
- 공식 원문 링크
- 전체 관리 상태
- 현재 전형
- 전형별 상태·일정·결과·메모
- 공고 메모
- 개인 일정
- 수정·삭제

상세에서 원본 데이터를 변경하면 저장 성공 후 대시보드와 캘린더 쿼리를 다시 계산한다. 캘린더 수정도 같은 공고·전형 레코드를 갱신한다.

## 7. 날짜·시간 규칙

- 날짜는 `YYYY-MM-DD`, 시각은 `HH:mm`, 기준 시간대는 `Asia/Seoul`이다.
- 날짜만 아는 일정은 `time: null`로 둔다.
- 마감 시각이 있으면 현재 KST와 비교해 일·시간·분을 표시한다.
- 날짜만 있는 마감은 마감일 당일에 `오늘 · 시간 미공개`로 표시하고 마감으로 단정하지 않는다.
- 날짜만 있는 마감은 다음 날 00:00 KST부터 지난 일정으로 분류한다.
- 시작일만, 마감일만 있는 공고를 허용한다.
- 시작 날짜가 종료 날짜보다 늦으면 저장하지 않는다.
- 같은 날짜에 양쪽 시각이 있을 때 시작 시각이 종료 시각보다 늦으면 저장하지 않는다.
- 월말·연말을 넘는 기간도 날짜 범위로 연속 표시한다.
- 미확정 일정은 `tentative: true`로 표시하고 ‘예정’ 라벨을 붙인다.

## 8. 데이터 요구사항

상세 타입과 관계는 [DESIGN.md](./DESIGN.md)를 따른다. 핵심 원본은 다음과 같다.

- `JobApplication`: 공고 기본 정보, 모집 일정, 전체 상태, 현재 전형, 메모
- `ApplicationStage`: 전형 상태, 일정, 결과, 메모
- `PersonalEvent`: 사용자가 직접 만든 공고 관련 일정
- `DerivedCalendarEvent`: 위 세 원본에서 화면 표시 시 생성되며 별도 저장하지 않음

모든 레코드에는 안정적인 로컬 UUID를 사용한다. 공식 식별값은 선택 값이며 로컬 ID를 대신하지 않는다.

## 9. 로컬 저장과 마이그레이션

목표 로컬 MVP 저장소는 `.local-data/jobs.json`이다.

- 스키마 버전을 기존 `1`에서 `2`로 올린다.
- 첫 v2 읽기 전에 `jobs.v1.<timestamp>.backup.json`을 만든다.
- v1 원본은 백업 성공 전까지 수정하지 않는다.
- 변환 성공, v2 검증 성공, 임시 파일 쓰기 성공 뒤에만 원자적으로 교체한다.
- 손상된 JSON은 초기화하지 않고 읽기 오류와 복구 위치를 알린다.
- 모든 쓰기는 기존 전역 순차 큐를 유지한다.

v1 변환:

- `Posting.id`, 제목, URL, 모집 일정, 메모, 출처를 보존한다.
- `stage: 관심` → `managementStatus: 관심`
- `stage: 지원 준비` → `managementStatus: 지원 준비`
- `stage: 지원 완료` → `managementStatus: 지원 중`으로 변환하고 기본 ‘서류 제출’ 전형을 `완료`로 둔다.
- 다른 기본 전형은 `미진행`으로 생성하되 결과와 날짜는 만들지 않는다.
- 공식 공고는 기존 공식 식별값을 가능한 경우 보존하고, 수동 공고는 `officialPostingId: null`로 둔다.
- 기존 데이터와 v2가 함께 있으면 v2를 기준으로 읽고 v1은 백업·가져오기 후보로만 표시한다.

MVP 내보내기는 v2 JSON 파일 다운로드 또는 지정 경로 복사로 제공한다. 가져오기는 별도 검증과 중복 미리보기 없이는 기존 데이터를 덮어쓰지 않는다.

## 10. 예외와 피드백

- 검색 중: 입력과 검색 버튼을 잠그고 진행 문구 표시
- 검색 결과 없음: 지원 범위와 직접 등록 경로 안내
- 일부 공식 공고 분석 실패: 성공 후보는 유지하고 누락 가능성 안내
- 저장 중: 동일 동작 재클릭 방지
- 저장 성공: 짧은 상태 메시지와 화면 즉시 갱신
- 저장 실패: 입력을 보존하고 기존 저장 파일을 유지
- 삭제: 연결 전형·일정 수를 포함한 확인 창 제공
- 중복 의심: 기존 공고와 비교 정보를 보여주고 취소 또는 계속 등록 선택
- 날짜 오류: 문제 필드 옆에 원인 표시

## 11. 범위

### 11.1 확장 MVP 필수

- v2 모델과 안전한 v1 마이그레이션
- 공식 검색·단일 선택 회귀 유지
- 수동 공고 등록·수정·삭제
- 전체 관리 상태와 전형 상태
- 기본·사용자 지정 전형
- 전형 일정과 개인 일정
- 확장 대시보드
- 월간·주간·목록 캘린더
- 필터, 반응형, 키보드 접근성
- JSON 내보내기

### 11.2 추후 확장

- 이메일·문자·푸시 알림
- 외부 캘린더 연동
- 백그라운드 예약 갱신
- AI 공고 분석
- 지원서 자동 제출
- 다중 사용자 공유와 권한
- 모든 기업을 대상으로 한 범용 크롤러

## 12. 인수 기준과 테스트 시나리오

| 입력·행동 | 기대 결과 |
|---|---|
| 지원되지 않는 기업명과 공고명만 직접 입력 | 일정 미공개 상태로 저장되고 개인 보드에 표시 |
| 시작일만 입력 | 마감일을 만들지 않고 시작 일정만 표시 |
| 마감일만 입력하고 시각 비움 | `time: null`, `시간 미공개`로 표시 |
| 같은 기업의 다른 차수·직무를 등록 | 각자 다른 로컬 ID로 둘 다 저장 |
| 공식 검색 후 후보 하나 선택 | 선택한 공고만 저장되고 다른 후보는 보드에 없음 |
| 모집 일정을 상세에서 수정 | 대시보드와 캘린더가 같은 값 표시 |
| 서류 제출을 완료로 변경 | 서류 상태는 완료, 합격으로 자동 변경되지 않음 |
| 필기를 현재 단계로 선택 | 기존 현재 단계 해제 후 필기만 현재 단계가 됨 |
| AI 역량검사를 해당 없음 처리 | 현재 단계 후보와 기본 일정 표시에서 제외 |
| 사용자 전형 추가·순서 변경 | 전형 ID, 기존 일정, 결과, 메모 유지 |
| 모집 마감 경과 | 전체 상태 `지원 중`은 불합격으로 바뀌지 않음 |
| 면접 일정 등록 | 공고 상세와 캘린더에 동일 일정 표시 |
| 캘린더에서 일정 수정 | 연결된 공고 또는 전형 원본이 갱신 |
| 같은 날짜에 셀 한도 이상 일정 존재 | 일부와 `+N개 더보기`가 표시되고 모두 조회 가능 |
| 월말을 넘는 모집 기간 등록 | 양쪽 달력에 연속 기간으로 표시 |
| 기업·일정 유형 필터 사용 | 조건에 맞는 일정만 표시하고 빈 상태 제공 |
| 앱 재실행 | 공고·전형·일정·메모 유지 |
| v1 JSON 마이그레이션 | 공고·URL·일정·메모 보존, 지원 완료는 지원 중/서류 완료로 변환 |
| 손상 JSON 읽기 | 원본을 덮어쓰지 않고 복구 가능한 오류 표시 |
| PC·모바일·키보드 사용 | 주요 추가·상태 변경·캘린더 이동·상세 열기 가능 |

## 13. 품질 및 검증

현재 설치된 스크립트는 `npm run lint`, `npm run build`, `npm run db:generate`이다. `typecheck` 스크립트는 현재 없다. 구현 단계에서는 다음 순서로 검증한다.

1. 변경 모델과 마이그레이션 단위 검증
2. 공식 검색·선택 회귀 검증
3. 수동 CRUD와 날짜 경계 검증
4. 전형 상태 불변식 검증
5. 캘린더 파생 이벤트 검증
6. `npm run lint`
7. 필요하면 `npx tsc --noEmit`
8. `npm run build`
9. PC·모바일 폭과 키보드 흐름 수동 확인

실제로 실행하지 않은 검증은 완료로 기록하지 않는다.

## 14. 개발 순서

1. 저장소 방향 결정: 로컬 JSON 복귀 또는 현재 D1 유지
2. v2 공고·전형·일정 타입과 불변식 정의
3. v1 백업·마이그레이션·롤백
4. 수동 공고 등록·수정·삭제
5. 전체 관리 상태와 전형 CRUD
6. 전형 일정·결과·개인 일정
7. 공고 상세 화면
8. 대시보드 확장
9. 월간 캘린더
10. 주간·목록 보기
11. 기업·일정 유형 필터
12. 반응형·접근성·예외 처리
13. 공식 조회 회귀와 전체 검증

## 15. 주요 위험과 대응

- **저장소 방향 충돌:** 구현 전에 로컬 JSON과 D1 중 기준을 확정하고 이중 기록을 만들지 않는다.
- **마이그레이션 손실:** 원본 백업과 v2 검증이 끝난 뒤에만 교체한다.
- **상태 모순:** 전체 상태와 전형 상태를 분리하고 자동 추론을 금지한다.
- **일정 중복:** 캘린더 이벤트를 원본 데이터에서 파생한다.
- **공식 사이트 구조 변경:** 기업별 오류를 보여주며 이미 저장한 사용자 데이터는 유지한다.
- **시각 미공개:** `null`을 유지하고 마감일 당일에 마감으로 단정하지 않는다.
- **복잡한 화면:** 카드에는 요약만, 상세 화면과 단계 편집은 필요할 때 연다.
