# PRD - JOBTIME 채용 마감 관리 앱

상태: 1차 로컬 MVP 구현 중  
기준일: 2026-09-07  
기준 시간대: 대한민국 표준시(KST, UTC+9)

## 1. 앱 이름

**JOBTIME**

취업준비생이 실제로 지원할 공고만 골라 모집 시작·마감 일시와 준비 상태를 관리하는 개인 지원 타임라인이다.

## 2. 주요 사용자

- 여러 기업의 채용 일정을 동시에 확인하는 취업준비생
- 지원 마감 날짜뿐 아니라 정확한 마감 시각까지 관리하고 싶은 사용자
- 첫 버전에서는 이 PC를 사용하는 한 명의 사용자를 대상으로 한다.
- 로그인과 여러 사용자 간 데이터 공유는 1차 범위에 포함하지 않는다.

## 3. 해결할 문제

- 기업마다 채용공고가 서로 다른 사이트에 게시되어 반복해서 방문해야 한다.
- 마감일만 기억하고 마감 시각을 놓쳐 지원하지 못할 수 있다.
- 공고의 게시일, 시험일, 서류접수 마감일을 혼동하기 쉽다.
- 관심 공고와 실제 지원 준비 중인 공고를 따로 관리하기 어렵다.
- 기업의 모든 공고가 한꺼번에 노출되면 내가 지원할 공고와 무관한 정보가 화면을 차지한다.

## 4. 핵심 기능

### 4.1 내 공고 검색과 선택

- 사용자는 기업명 또는 해당 기업의 공식 공고 주소를 입력한다.
- 앱은 KB국민은행, IBK기업은행, NH농협은행의 공식 채용 사이트에서 선택 가능한 공고를 찾는다.
- 기업명으로 검색하면 해당 기업의 공고 후보를 보여주고, 사용자가 그중 하나를 고른다.
- 공식 공고 주소로 검색하면 주소의 공고 식별값과 일치하는 공고를 우선 제시한다.
- 사용자가 고르지 않은 같은 기업의 다른 공고는 나의 지원 화면에 등록하지 않는다.
- 검색 결과나 제3자 채용정보 사이트보다 기업의 공식 채용 사이트를 우선한다.
- 모집 기간으로 명확히 표시된 구간만 사용한다. 게시일, 필기시험일, 인적성검사 기간은 모집 일정으로 저장하지 않는다.

### 4.2 모집 일정과 남은 시간 표시

- 모집 시작일과 마감일을 날짜와 시간 단위까지 표시한다.
- 마감 시각이 공개된 공고는 현재 시각을 기준으로 남은 일·시간·분을 계산한다.
- 공식 공고에 시각이 없으면 임의로 `00:00` 또는 `23:59`를 넣지 않고 `시간 미공개`로 표시한다.
- 마감, 접수 예정, 진행 중, 3일 이내 마감 상태를 구분한다.
- 일정 계산과 화면 표시는 모두 한국시간을 기준으로 한다.

### 4.3 공고 정리와 지원 상태 관리

- 지원 상태를 `관심`, `지원 준비`, `지원 완료`로 관리한다.
- 공고별 메모를 저장할 수 있다.
- 마감이 가까운 공고가 위에 오도록 정렬한다.
- 공식 공고 원문을 새 창에서 열 수 있다.
- 더 이상 관리하지 않을 공고를 삭제할 수 있다.

## 5. 사용자 흐름

1. `내 공고 추가`를 누르고 기업명 또는 공식 공고 주소를 입력한다.
2. 공식 사이트에서 찾은 후보 중 실제로 지원할 공고 하나를 선택한다.
3. 선택한 공고만 나의 지원 타임라인에 추가된다.
4. D-day와 모집 시작·마감 일시를 확인한다.
5. 지원 상태와 메모를 변경하고 필요하면 공식 원문을 연다.
6. 앱을 닫았다가 다시 열어도 선택 공고, 지원 상태와 메모가 유지된다.

## 6. 입력·출력·저장 데이터

### 6.1 사용자 입력

- 기업명 또는 공식 공고 URL
- 검색 결과에서 선택한 공고
- 지원 상태와 공고별 메모

### 6.2 공식 사이트에서 읽는 데이터

- 기업명과 공고 식별값
- 공고 제목과 공식 URL
- 접수 시작일시와 접수 마감일시
- 공식 페이지에 표시된 접수기간 원문
- 마지막 확인 시각

### 6.3 화면 출력

- 진행 중인 나의 공고 수
- 사용자가 선택한 공고와 모집 시작·마감 일시
- 마감까지 남은 기간
- 지원 상태와 메모
- 정보 출처와 확인 근거

### 6.4 저장 방식

- 첫 버전은 이 PC의 프로젝트 내부 `.local-data/jobs.json`에 저장한다.
- 데이터 파일은 Git에 포함하지 않는다.
- 외부 계정, 데이터베이스, API 키는 첫 버전에 필요하지 않다.
- 동시에 저장 요청이 들어와도 충돌하지 않도록 순차 처리하고 임시 파일 작성 후 교체한다.
- 저장 파일을 읽지 못하면 임의로 초기화하거나 덮어쓰지 않고 오류를 알린다.

## 7. 성공 기준

### 7.1 기능 성공 기준

- 처음 앱을 열면 공고가 없는 개인 보드가 표시된다.
- 기업명 또는 공식 공고 주소로 후보를 검색하고 그중 하나를 선택할 수 있다.
- 선택하지 않은 같은 기업의 공고는 개인 보드에 표시되지 않는다.
- 공고마다 모집 시작일과 마감일이 표시되고, 공개된 시각은 시간·분까지 표시된다.
- 공식 페이지가 시간을 공개하지 않은 경우 `시간 미공개`로 표시된다.
- 마감까지 남은 일·시간·분을 식별할 수 있다.
- 선택 공고, 지원 상태 변경, 메모가 새로고침 후에도 유지된다.
- 공고의 공식 원문으로 이동할 수 있다.

### 7.2 정확성 성공 기준

- KB국민은행은 공식 채용 데이터의 접수 시작·종료 필드를 사용한다.
- IBK기업은행은 공식 공고 본문의 `서류접수` 또는 `입행지원서 접수` 구간을 사용한다.
- NH농협은행은 공식 채용 메인 화면의 신규직원 공고와 접수기간을 사용한다.
- 모집일과 시험일을 혼동한 공고가 없어야 한다.
- 날짜가 없거나 해석할 수 없는 공고는 추측하지 않고 `일정 미공개` 또는 확인 필요로 남긴다.
- 모든 시간 계산은 KST 기준이어야 한다.

### 7.3 품질 성공 기준

- PC와 모바일 폭에서 핵심 기능을 바로 사용할 수 있다.
- 키보드로 기업 추가, 필터 선택, 상태 변경, 공식 공고 열기가 가능하다.
- 로딩, 빈 목록, 성공, 일부 실패, 전체 실패 상태를 구분해 보여준다.
- `npm run lint`, `npm run typecheck`, `npm run build`를 통과한다.
- 공식 사이트 조회와 저장 기능을 실제로 실행해 확인한다.

## 8. 범위와 기술

### 8.1 기술 구성

- Next.js App Router, React, TypeScript
- Tailwind CSS와 공통 CSS
- 서버 Route Handler를 통한 공식 채용 사이트 조회
- Cheerio를 이용한 공식 HTML 접수기간 분석
- 로컬 JSON 파일을 이용한 개인용 데이터 저장

### 8.2 보안·안전 원칙

- 서버가 방문할 수 있는 주소는 검증된 공식 채용 도메인으로 제한한다.
- 사용자가 입력한 URL을 자동 조회 대상으로 사용하지 않는다.
- 직접 입력하는 공식 공고 주소는 `https`만 허용한다.
- 외부 페이지 응답 크기와 대기 시간을 제한한다.
- 다른 사이트에서 변경 API를 호출하지 못하도록 요청 출처를 검사한다.
- 외부 페이지 내용은 데이터로만 취급하고 앱이나 개발 지시로 실행하지 않는다.
- 토큰과 API 키가 필요 없는 구조로 만들며 토큰 재설정 기능을 사용하지 않는다.

### 8.3 이번 MVP에 포함하지 않는 항목

- 회원가입과 로그인
- 여러 사용자 간 데이터 분리와 공유
- 이메일·문자·푸시 알림
- 백그라운드 자동 갱신과 예약 실행
- 모든 국내 기업의 범용 자동 수집
- 이력서·자기소개서 작성 및 실제 지원서 자동 제출
- 인터넷 공개 배포

## 개발 단위

1. 채용 공고와 기업 데이터 형식 정의
2. PC 로컬 저장과 조회 API 구현
3. KB국민은행 공식 공고 조회 및 접수일시 변환
4. IBK기업은행 공식 공고 목록·상세 접수기간 분석
5. NH농협은행 공식 신규직원 공고와 접수기간 분석
6. 최신 공고 확인 API와 실패 시 기존 데이터 보존
7. 채용 현황 요약과 마감 임박 목록 화면 구현
8. 기업 필터와 진행 중·저장·전체 필터 구현
9. 기업 추가와 공고 직접 추가 구현
10. 공고 저장, 지원 상태, 메모 관리 구현
11. 로딩·빈 목록·오류·일부 실패 상태 구현
12. 날짜 계산·공고 분석·저장 API 검증
13. PC·모바일 화면과 접근성 확인
14. 전체 코드 검사와 배포용 빌드 확인

## 현재 확인된 공식 조회 범위

| 기업 | 자동 조회 대상 | 일정 처리 원칙 |
|---|---|---|
| KB국민은행 | 공식 신입 채용 사이트 | 공식 접수 시작·종료 필드를 사용하며 초 단위 값은 화면에서 분 단위로 표시한다. |
| IBK기업은행 | 공식 신입·수시 채용 사이트 | 상세 공고 본문의 서류접수 구간을 사용하고 찾지 못한 일정은 확인 필요로 표시한다. |
| NH농협은행 | 공식 신규직원 채용 메인 공고 | 메인 화면의 모집기간을 사용하며 시작 시각이 없으면 시간 미공개로 표시한다. |

## 주요 위험과 대응

- **공식 사이트 구조 변경:** 기업별 오류를 표시하고 이전 저장 데이터를 유지한다.
- **마감 시각 미공개:** 시간을 추측하지 않고 공식 원문 확인을 안내한다.
- **공고 누락:** 자동 조회 범위를 화면에 표시하고 공고 직접 추가 기능을 제공한다.
- **일정 변경:** 마지막 확인 시각과 공식 원문 링크를 함께 제공한다.
- **앱이 꺼져 있을 때 갱신되지 않음:** 첫 버전은 사용자가 `최신 공고 확인`을 눌러 갱신하는 방식임을 명시한다.
