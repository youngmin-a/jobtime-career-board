import {newStage,type Application} from './applications';
export const standardStages=['서류 제출','AI 역량검사','필기 전형','1차 면접','2차 면접','최종면접'];
export type Column={id:string;name:string;kind:'preparing'|'stage'|'accepted'|'archived'};
export function columns(apps:Application[]):Column[]{const names=[...new Set([...standardStages,...apps.flatMap(a=>a.stages.filter(s=>s.applicable).map(s=>s.name))])];return[{id:'preparing',name:'준비 중',kind:'preparing'},...names.map(name=>({id:'stage:'+name,name,kind:'stage' as const})),{id:'accepted',name:'최종합격',kind:'accepted'},{id:'archived',name:'종료 / 보관',kind:'archived'}]}
export function columnFor(a:Application){if(a.managementStatus==='accepted')return'accepted';if(['rejected','withdrawn'].includes(a.managementStatus))return'archived';const stage=a.stages.find(s=>s.id===a.currentStageId&&s.applicable);return stage?'stage:'+stage.name:'preparing'}
export function moveApplication(a:Application,column:Column):Application{const next=structuredClone(a);if(column.kind==='accepted'){next.managementStatus='accepted';return next}if(column.kind==='archived')throw new Error('불합격 또는 지원 철회를 직접 선택해주세요.');if(column.kind==='preparing'){next.currentStageId=null;next.managementStatus='preparing';return next}let stage=next.stages.find(s=>s.name===column.name&&s.applicable);if(!stage){stage=newStage(column.name,next.stages.length);next.stages.push(stage)}next.currentStageId=stage.id;next.managementStatus='active';return next}
